# croton.news — Full Site & Code Audit

**Date:** 2026-07-13 · **Status:** IN PROGRESS (ops audit complete; code audit + live-site UX audit pending)

This is a living document. Findings are ranked by severity. Checkboxes track remediation.

---

## Incident log (context)

**2026-07-13 21:03–21:19 UTC — Site outage.** During sshfs mount setup, `/opt/seo-audit-tool/server.js` was started with `PORT=3260` (croton.news's port). Flask crash-looped 172 times (`Restart=always` masked it); croton.news publicly served the SEO tool. Resolved 21:19 — rogue process died/killed, Flask rebound, verified serving publicly. **Root enabler:** two competing restart procedures (systemd unit vs. `nohup` recipe in CLAUDE.md) and no port-conflict alerting.

---

## CRITICAL

### C1. Secrets exposed — rotate everything
- [ ] `/etc/systemd/system/croton-news.service` is **mode 644** with plaintext `Environment=` API keys: `GROQ_API_KEY`, `OPENROUTER_API_KEY`, `GEMINI_API_KEY`, `DEEPGRAM_API_KEY`
- [ ] `openrouter_credentials.json` **tracked in git and pushed** to github.com/keptlive/croton-news (commit `6de2d3b`); `.gitignore` rule added too late to matter. Also 644 on disk.
- [ ] Git remote URL in `.git/config` embeds a **GitHub OAuth token** (`gho_…`)
- [ ] `rag/.env` is 644 (should be 600); root `.env` is 600 ✓

**Remediation:** rotate all 4 provider keys + GitHub token (OWNER ACTION) → move keys to a 600 `EnvironmentFile` → purge credentials file from git history before any future push → chmod hardening (see Quick wins).

### C2. Half the content pipeline silently dead since 2026-05-09
- Cron jobs removed: `pipeline.py process-new` (7:00), `boarddocs.py sync` (7:15), `poll_boe.py` (7:30), `story_miner.py` (8:00), hourly `auto_pipeline.py`. All their logs end May 9.
- **Board of Education coverage dead** — last BOE meeting in rag.db: 2026-05-19. Nothing polls BoardDocs or YouTube.
- `static/upcoming_agendas.json` **frozen since May 9** — site served stale "upcoming" data for 2 months.
- Partial replacement `rag/auto_discover.sh` (daily 6:00) works but logs to volatile `/tmp/croton-discover.log` and has recurring unresolved matcher warnings (BOT 2026-06-17, WCC 2025-10-20).
- Pre-existing failures: `youtube-transcript-api RequestBlocked`, `yt-dlp` needs cookies (VPS IP blocked — phone relay documented in CLAUDE.md).
- [ ] Decide: restore old jobs vs. extend `auto_discover.sh`; move log out of /tmp; fix CLAUDE.md cron table.

### C3. Competing restart procedures (caused tonight's outage pattern)
- CLAUDE.md documents `fuser -k 3260/tcp; nohup python app.py &` while systemd runs `Restart=always`. Any manual start races the unit.
- [ ] Single restart path: `systemctl restart croton-news` only; remove nohup recipe from CLAUDE.md.

### C4. Raw `{{quote:N}}` tags render as literal text in articles — ROOT CAUSE FOUND
- **No server-side renderer exists for `{{quote:…}}`.** `{{photo:…}}` is rendered by the `process_photos` Jinja filter (app.py:199-239, applied at templates/article.html:177), but quotes are rendered ONLY by client-side JS (inline IIFE, article.html:179-316).
- Served HTML contains literal tags → seen by curl, RSS readers, crawlers, no-JS clients; flash-of-raw-tags on every load.
- The IIFE is fragile: the lookbehind regex at article.html:280 is a **parse-time SyntaxError on Safari < 16.4** — on those iPhones the whole renderer never runs and every tag stays raw. (JS logic itself verified correct in headless Chromium.)
- **Fix:** add a `process_quotes` Jinja filter mirroring `process_photos` (handle `{{quote:SECONDS}}`, `{{quote:EVENT:SECONDS}}`, `{{quote:yt-ID:SECONDS}}`), chain it at article.html:177; keep JS as progressive enhancement.
- Related: `rag/publish_article.py:38,41` has a fatal **SyntaxError** (`re.findall(r{{quote:d+}}, …)` — missing quotes/escapes), so its orphan-tag-stripping safeguard never runs.
- Minor: single-number quote timestamps are LLM-approximate (~74s off in article 144).
- [ ] Implement `process_quotes` filter · [ ] fix publish_article.py · [ ] consider timestamp snapping to nearest chunk

### C5. Destructive admin endpoints are completely unauthenticated
- No auth anywhere in app.py (no session/login_required/before_request/token). Anyone can call:
  - `DELETE /api/photos/<id>` (app.py:1999) — deletes DB rows AND `os.remove()`s files
  - `POST /api/photos/<id>/feature` (:2017), `POST /api/photos/<id>/tags` (:2027)
  - `DELETE /api/comments/<id>` (:2094)
  - `POST /api/indexnow` (:1882)
- `/status` (:2455) publicly dumps log tails + `crontab -l`; `/review` (:2841) exposes unpublished drafts.
- [ ] Gate all mutating/admin routes (shared-secret header or session login); then CSRF tokens on form posts.

### C1b. (extends C1) OpenRouter keys retrievable from git HEAD
- `git show HEAD:openrouter_credentials.json` returns live `sk-or-v1…` keys (both `openrouter_api_key` and `openrouter_paid_key`). File's own notes admit a prior key leaked this same way.
- [ ] `git rm --cached openrouter_credentials.json` + rotation (OWNER) + history purge before any push.

## HIGH

### H1. Backups: brand new, same-disk, single-DB
- Only 2 backups, both from today (2026-07-13). One-line backup log. Script untracked in git.
- [x] **Verified 2026-07-13**: script uses sqlite `.backup` (WAL-safe, correct); backup passes `quick_check`, table counts match live as of backup time. Size gap (113MB vs 337MB) explained: +4,399 chunks and +17,449 Gemini embeddings were ingested later the same day (retranscription of ~12 meetings incl. 2 BOE YouTube videos).
- Same disk as production (`/dev/vda2`); no offsite copy.
- Not covered: `comments.db`, `tips.db`, `photos.db`, `ecode360/summaries.db`, `rag/history/*.db`, `.env` files.
- [ ] Add remaining DBs, add offsite destination, commit backup_db.sh.

### H2. Production DB is a symlink named "test"
- `rag/rag.db -> rag-test.db` (since Apr 8), alongside stale `rag.db.testing`, `rag-testing.db` (72MB each), 0-byte `rag.db.bak`. A "test file cleanup" could destroy production.
- DB itself healthy: `quick_check` ok; 139 meetings; latest 2026-07-01; 0 transcripts missing articles.
- [ ] Make rag.db a real file; archive/delete the stale test copies (after backup verified).

### H3. ~7 weeks of production code uncommitted
- Last commit 2026-05-26. 19 modified files (+894/−63) incl. app.py, pipeline scripts, 14 templates; untracked: `rag/backup_db.sh`, `rag/publish_article.py`, `rag/retranscribe.py`, `rag/rebuild_code_db.py`, `templates/404.html`, `static/opensearch.xml`.
- [ ] Checkpoint commit on VPS. **Do not push** until C1 history purge is done.

### H4. Flask dev server (werkzeug) serving production
- `ExecStart=…python app.py` → `app.run()` (app.py:3258); journal shows the dev-server warning. Single process. Service at 901MB RSS / 1.1GB peak on a 3.3GB box, 1.1GB swap used, 151MB free.
- [ ] Move to gunicorn (2 workers, timeout tuned), add `MemoryMax=` to unit. Runs as root — add `User=` while at it.

### H5. `/documents` search always returns zero results
- app.py:721-725 — result-building loop is indented under `except Exception:` instead of after the try. Success path never populates `results`; every query silently returns empty.
- [ ] Dedent lines 721-725.

## MEDIUM

- **M6.** Comments auto-approve (`approved=1` default, app.py:2050) — spam publishes instantly. Honeypot + 5/hr/IP rate limit + tag-stripping exist; XSS contained (autoescape). Fix: default `approved=0` + moderation queue.
- **M7.** `/tips` POST (app.py:1354) has no rate limit/honeypot — unlimited anonymous inserts.
- **M8.** `/status` runs 6 subprocess tails + crontab + two 8s external HTTP probes per page load, uncached — slow and abusable while public (fix with C5 + ~60s cache).
- **M9.** `snippet|safe` renders LLM/DB text unescaped (documents.html:66, document_detail.html:77, villagecode.html:537) — sanitize before `|safe`.

**Verified NOT vulnerable:** SQL injection (all parameterized; allowlisted f-strings only), path traversal (`send_from_directory` + regex-validated filenames), `/api/openverse` SSRF (hardcoded host).

- **M1.** Newest article is 2026-07-01; meetings since (Jul 7 Arts Council, Jul 10 IDEA) uncovered — consequence of C2.
- **M2.** AI-generated-content disclaimer buried in footer small print; per-article notice near byline recommended (trust + accuracy expectations).
- **M3.** `python-dotenv` not installed — app depends solely on systemd `Environment=` lines; C1 remediation must account for this.
- **M4.** Search results expose internal relevance scores ("27.1") to readers.
- **M5.** Transcription artifacts surface publicly (e.g. "lead blowers" for "leaf blowers") — enrichment pass gap.

## LOW / CLUTTER

- **L1.** Ten 0-byte vestigial DBs at project root (`app.db`, `articles.db`, `croton.db`, `croton_news.db`, `history.db`, `news.db`, `rag.db`, `search.db`, `site.db`, `summaries.db`) — referenced by nothing; safe to delete. NOTE: root `tips.db`, `photos.db`, `comments.db` ARE live.
- **L2.** 14 `.bak` files (app.py.bak.*, template .baks) — valueless once H3 commit lands.
- **L3.** CLAUDE.md drift: cron table (C2), restart procedure (C3 — fixed 2026-07-13).
- **L4.** `?limit=abc` → 500 on `/api/articles` (app.py:1812) and `/api/search` (:1547) — unguarded `int()` casts.
- **L5.** Dead code: literal doc-string cleanup in `process_photos` (app.py:237-238).
- **L6.** Vector search loads all embeddings into RAM per corpus (cached; fine at 22k, watch growth).

## Healthy / verified good

- Site fast: 0.08–0.4s TTFB across main pages.
- RSS scrapers current (last run Jul 13 18:00); daily backup + weekly video cleanup crons running.
- rag.db integrity ok; article coverage complete for ingested meetings.
- Search quality strong (FTS + semantic, speaker/timestamp attribution).
- Disk 58% used (26GB free); load low.

---

## Pending audit results

- [x] Code audit (app.py, templates, security) — complete, folded in above
- [x] Live-site UX audit — complete, see below

## Live-site UX audit findings (2026-07-13)

**HIGH**
- **U1. Dead media links, systemic**: 16/16 sampled `▶ Video` links to `/videos/<id>.mp4` on search/topic/entity/article pages are 404 (only recent meetings 1161+ have video files); 5 `/audio/<id>.mp3` also 404, leaving dead `<audio>` widgets. Fix: only render media links when file exists (or check a has_video flag).
- **U2. `/status` hard-down**: HTTP/2 PROTOCOL_ERROR / empty reply — server-side hang (likely the 6 subprocess tails + 8s external probes on a single-threaded dev server). Now behind auth (2026-07-13); still needs caching + fix.
- **U3. `/review` drafts were public + indexable** — unpublished draft w/ internal metadata (model, source file) publicly served, no noindex. **Fixed 2026-07-13: behind ADMIN_TOKEN.**

**MEDIUM**
- **U4.** Literal `None` bullets in "Key Actions & Decisions" panels (/article/144: 24×, /article/143: 19×) — Python None leaking into template lists.
- **U5.** Raw generator scaffolding (`=== HEADLINE ===`, `=== SUMMARY ===`) visible on /topic/village-budget-2026-2027 (one bad stored summary; also odd article ID /article/753225933).
- **U6.** Article bodies ship as raw markdown headings (`## `) transformed client-side — same architecture issue as C4; quote tags now fixed server-side, markdown still JS-only.
- **U7.** Page weight: /gallery 4.8MB HTML (1,179 imgs, no width/height); /entities 548KB (1,015 links, no pagination); /history 3.3s response.
- **U8.** Meetings taxonomy polluted: "📰 Topics" pseudo-committee, empty "Other" filter, dupes (PAC ×2, "Planning Board"/"Planning Board Meeting"), one-off events as committees.

**LOW**
- **U9.** "COMING UP" cards duplicate committee name as title; meta descriptions are raw agenda concatenations (mismatch headline, truncate mid-word); "1 documents"; markdown asterisks on /history subtitles; garbled transcript snippets in search; news-sitemap uses -05:00 in summer (EDT is -04:00).

**Verified good**: 108 internal URLs all 200 (except media above); RSS/sitemaps/robots well-formed; full OG/Twitter/geo meta + canonicals; viewport everywhere; homepage weight modest; content fresh.

## Quick wins applied

- **2026-07-13** — chmod 640 `croton-news.service`, chmod 600 `rag/.env` + `openrouter_credentials.json`; site verified up after.
- **2026-07-13** — CLAUDE.md: replaced `fuser -k`/`nohup` restart recipe with `systemctl restart croton-news` (fixes C3 doc side).
- **2026-07-13** — Backup restore-verified (see H1). Key rotation (C1): **owner is handling personally** — do not rotate from automation.
- **2026-07-13** — Checkpoint commit `dc2c0c7` (7 weeks of changes; backups/ + *.bak gitignored). NOT pushed.
- **2026-07-13** — **C4 fixed**: server-side `process_quotes` Jinja filter added (app.py, mirrors JS renderer incl. yt-/explicit/implicit forms + consecutive-duplicate collapse); chained in article.html. Verified: 0 raw tags in served HTML, 8 rendered spans on /article/144. JS renderer left as no-op fallback. `publish_article.py` SyntaxErrors fixed (regex quotes + SQL `''` literals).
- **2026-07-13** — **C5 fixed**: `require_admin` (ADMIN_TOKEN in root `.env`, 600) gating DELETE/POST photo+comment+indexnow APIs, /status, /review. Verified 403 without token, 200 with. Browser flow: `?token=...` once → cookie.
- **2026-07-13** — **H5 fixed**: /documents search dedent — verified results with `<mark>` snippets for q=budget.
- **2026-07-13** — **M7/L4 fixed**: /tips honeypot + 5/hr/IP rate limit + field length caps; `type=int` guards on /api/articles + /api/search (limit=abc → 200).
- **2026-07-13** — **U1/U4 fixed** (`fcc0245`): `md_bold` filter was a body-less stub returning `None` → every Key Actions bullet site-wide rendered as the literal string "None". Implemented. `has_video`/`has_audio`/`media_video_href` globals gate all media links on file existence (videos pruned weekly); yt- meetings link to YouTube. Verified: 0 dead links on search page, 0 None spans, real bold bullets.
- **2026-07-13** — **C2/C3 remediated** (`9fb8a99`): pipeline reliability layer. `notify.py` (email alerts to ALERT_EMAIL w/ cooldowns), `run_job.sh` (every cron job → job_runs.db + failure email w/ manual-fix hint), `pipeline_watch.py` (daily 9:00 outcome checks + Monday digest; checks CHUFSD YouTube RSS and emails phone-relay actions). Dead jobs restored to cron: boarddocs-sync 7:15, boe-poll 7:30, upcoming-agendas hourly. auto_discover.sh → venv python + /var/log + per-stage failure exit. Logrotate added. WireClaw 8:00 enrichment cron emails on failure. Test email sent + received path verified. First watch run flagged 2 unprocessed BOE videos (Jun 4, Jun 11) — **phone relay action pending**.
- **2026-07-13** — dispatch_packet.py (WireClaw): task-id collision fix (same-second dispatches).
- **2026-07-13** — **H4 fixed** (`7571af2`): gunicorn (1w×8t, MemoryMax=2G), bound 127.0.0.1:3260 (no longer world-exposed); keys → `secrets.env` (600, EnvironmentFile, gitignored); worker RSS 594MB (was 901MB). **U2 fixed**: /status works under threads + 60s render cache (warm hits 1.6ms). *(Note: secrets.env was briefly committed by an over-broad `git add -A`, caught immediately and amended out — verified absent from all history; never pushed.)*
- **2026-07-13** — **H1/H2/L1/L2 fixed**: backup_db.sh covers all 5 DBs (keep 7); WireClaw pulls latest gzipped set nightly 5:40 with staleness alert (offsite at 107.173.0.190:/root/croton-backups). rag.db is a real file (symlink removed); test copies + 16 .baks → backups/attic; ten 0-byte DBs deleted.
- **2026-07-13** — **U5/U9-partial fixed** (`e44877e`): === HEADLINE === markers stripped from 64 article chunks + FTS rebuilt; news-sitemap DST offset via _tz_offset; "1 documents" pluralized.
- **2026-07-13** — CLAUDE.md rewritten: real cron table, Reliability & Alerting section, gunicorn/secrets docs.

**Remaining open:** key rotation + git history purge (owner), UX polish round 2 (pagination, taxonomy dedupe, CSRF), comments moderation default (owner decision), verify first automated cron cycle tomorrow ~9:15 UTC.
